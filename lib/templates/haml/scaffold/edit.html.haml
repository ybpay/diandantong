.header
  %h3= "编辑#{t('activerecord.models.ddt/<%= singular_table_name %>')}"

= render 'form'

= link_to t('Show'), [:backend, @current_shop, @<%= singular_table_name %>], :class => "btn btn-default btn-sm"
\
= link_to t('Back'), backend_shop_<%= index_helper %>_path(@current_shop), :class => "btn btn-default btn-sm"