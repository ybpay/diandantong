.row.wrapper.border-bottom.white-bg.page-heading
  .col-lg-10
    %h2= "#{t('activerecord.models.ddt/tag')} 列表"
    %p
      温馨提示：
      %br
      本页面不提供新建标签的功能， 您可以在编辑对象的时候，为对象打上标签。
      %br
      另外:当您删除一个标签的时候， 那些打上该标签的对象，也会对应地删除该标签，请谨慎操作。
.wrapper.wrapper-content.animated.fadeInRight.ecommerce
  .row
    .col-lg-12
      .ibox
        .ibox-content
          %table.table.table-striped
            %thead
              %tr
                %th.col-sm-1= sort_link(@q , :name , Ddt::Tag.human_attribute_name(:name))
                %th.col-sm-1= sort_link(@q , :type , Ddt::Tag.human_attribute_name(:type))
                %th.col-sm-1= sort_link(@q , :color , Ddt::Tag.human_attribute_name(:color))
                %th.col-sm-1= sort_link(@q , :count   , Ddt::Tag.human_attribute_name(:count))
                %th.col-sm-2

              = render 'tbody', { tags: @tags}
          %br
          .pull-right= will_paginate @tags , renderer: BootstrapPagination::Rails, bootstrap: 3
          %br
