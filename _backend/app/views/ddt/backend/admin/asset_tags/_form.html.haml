= simple_form_for([:backend, @asset_tag], html: { class: 'form-horizontal'}) do |f|
  = f.error_notification :message => error_message_in_html(@asset_tag)

  .form-inputs
    = f.input :name

  .form-actions.col-xs-offset-2
    = f.button :submit, :class => "btn btn-primary btn-sm"
