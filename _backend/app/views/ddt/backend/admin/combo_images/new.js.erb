bootbox.hideAll();
bootbox.dialog({
  message: "<%= j render 'form' %>",
  title: "新建<%= t('activerecord.models.ddt/combo_image')%>"
});
setTimeout(function(){
  $(document).trigger('page:load')
}, 500)
