bootbox.hideAll();
bootbox.dialog({
  message: "<%= j render 'form' %>",
  title: "编辑<%= t('activerecord.models.ddt/variant_image')%>"
});
setTimeout(function(){
  $(document).trigger('page:load')
}, 1000)
