bootbox.hideAll();
<%- @variant_images.each do |variant_image|%>
$(".variant-image-tr[data-id='<%= variant_image.id%>']").replaceWith("<%= j render partial: 'variant_image', locals:{ variant_image: variant_image }%>")
<%- end%>
