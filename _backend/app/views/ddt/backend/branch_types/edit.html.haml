.ibox
  .ibox-title
    %h3= "编辑#{t('activerecord.models.ddt/branch_type')}"
  .ibox-content
    .col-md-10
      = render 'form'
      = link_to t('Show'), [:backend, @current_shop, @branch_type], :class => "btn btn-default btn-sm"
      \
      = link_to t('Back'), backend_shop_branch_types_path(@current_shop), :class => "btn btn-default btn-sm"
    .clearfix


