.alert.alert-info
  %strong 关于角色分工说明：
  .p
    系统提供的角色是经过大部分商家的需求完善而来的，通过给账户添加对应的角色可以满足大部分商家的需求。然而我们还是无法避免有些商家对于某个角色有特殊的需求， 对于这些商家，我们建议您通过为一个帐号添加多个角色来加强某个帐号的权限

.row.wrapper.border-bottom.white-bg.page-heading
  .col-lg-10
    %h2= "#{t('activerecord.models.ddt/role')} 列表"

    .form-group
      = new_btn(:role)

.wrapper.wrapper-content.animated.fadeInRight.ecommerce
  .row
    .col-lg-12
      .ibox 
        .ibox-content
          %table.table.table-striped
            %thead
              %tr
                %th.col-sm-1= sort_link(@q, :display_name, Ddt::Role.human_attribute_name(:display_name))
                %th.col-sm-1= sort_link(@q, :builtin, Ddt::Role.human_attribute_name(:builtin))
                %th.col-sm-1

            %tbody
              - @roles.each do |role|
                %tr
                  %td= role.display_name
                  %td= role.builtin_name
                  %td
                    = info_btn(role)
                    - unless role.builtin?
                      = edit_btn(role)
                      = delete_btn(role)
              = no_record_hint(@roles)
          %br
          .pull-right= will_paginate @roles , renderer: BootstrapPagination::Rails, bootstrap: 3
          %br
