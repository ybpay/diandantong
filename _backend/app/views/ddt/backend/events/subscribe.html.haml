.ibox
  .ibox-title
    %h4 用户关注时自动回复以下内容
  .ibox-content
    .col-md-12
      .subscribe_event_show
        - event = @subscribe_event
        = render 'config_once_event_show', {event: event, event_type: 'subscribe'}
    .clearfix

