.block
  .ibox-title
    %h2= "编辑#{t('activerecord.models.ddt/printer')}"

  = render 'form'

  = link_to t('Back'), backend_shop_branch_printers_path(@current_shop, @current_branch), :class => "btn btn-default"
