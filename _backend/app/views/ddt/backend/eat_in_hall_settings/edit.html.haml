.ibox
  .ibox-title
    %h2= "编辑#{t('activerecord.models.ddt/eat_in_hall_setting')}"

    .alert.alert-info
      %p
        扫码堂点模式，是指客户用手机扫二维码进行堂点, 所采用的模式，分以下两种, 默认为第二种
        %br
        预付款模式：
        下单的时候不支持现金支付，必须在线支付，且付款后收银端才出单
        %br
        后付款模式：
        支持现金支付，下单即可出单
  .ibox-content
    = render 'form'

    = link_to t('Show'), backend_shop_branch_eat_in_hall_setting_path(@current_shop, @current_branch), :class => "btn btn-default btn-sm"
