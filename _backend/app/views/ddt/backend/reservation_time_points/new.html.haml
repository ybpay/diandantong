.ibox
  .ibox-title
    %h2= "新建#{t('activerecord.models.ddt/reservation_time_point')}"
  .ibox-content
    = render 'form'

    = link_to t('Back'), backend_shop_branch_reservation_time_points_path(@current_shop, @current_branch), :class => "btn btn-default btn-sm"