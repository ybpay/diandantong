zh-CN:
  simple_form:
    "yes": '是'
    "no": '否'
    required:
      text: '必填'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "请检查下面的问题："
    # Examples
    # labels:
    #   defaults:
    #     password: 'Password'
    #   user:
    #     new:
    #       email: 'E-mail to sign in.'
    #     edit:
    #       email: 'E-mail.'
    # hints:
    #   defaults:
    #     username: 'User name to sign in.'
    #     password: 'No special characters, please.'
    # include_blanks:
    #   defaults:
    #     age: 'Rather not say'
    # prompts:
    #   defaults:
    #     age: 'Select your age'
    hints:
      defaults:
        max_grant_limit: 最大可设置成1000, 000
      queue_setting:
        guest_num_le: 设置为自动排号时，当排号客户的用餐人数少于等于此人数时，系统将自动为排号客户分配此队列,
      branch:
        support_wifi: 若门店类型设置为不显示wifi，则该项不会显示在微信用户端
        support_parking: 若门店类型设置为不显示停车位，则该项不会显示在微信用户端
        parking_space_count: 若门店类型设置为不显示停车位，则该项不会显示在微信用户端
    placeholders:
      coupon_usage_instructions:
        content: 例如：团购用户不可同时享受商家其他优惠
      voucher_versions:
        groupon_price: 团购价
      coupons:
        user: 用户ID